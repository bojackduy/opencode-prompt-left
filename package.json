{
  "name": "@bojackduy/opencode-prompt-left",
  "version": "0.2.0",
  "description": "Estimates how many similar prompts remain before provider quota runs out, shown in the OpenCode compact line.",
  "type": "module",
  "license": "AGPL-3.0-only",
  "files": [
    "src",
    "assets",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test",
    "build": "bun build src/server.ts --target=bun --external @opencode-ai/plugin --external @opencode-ai/sdk --outfile /tmp/prompt-left-server.js && bun build src/tui.tsx --target=bun --external @opencode-ai/plugin/tui --external @opentui/solid --external solid-js --outfile /tmp/prompt-left-tui.js",
    "prepack": "bun run typecheck && bun test && bun run build",
    "release:patch": "bun run typecheck && bun test && npm version patch -m \"chore: release %s\" && git push && git push origin $(git describe --tags --abbrev=0)",
    "release:minor": "bun run typecheck && bun test && npm version minor -m \"chore: release %s\" && git push && git push origin $(git describe --tags --abbrev=0)",
    "release:major": "bun run typecheck && bun test && npm version major -m \"chore: release %s\" && git push && git push origin $(git describe --tags --abbrev=0)"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bojackduy/opencode-prompt-left.git"
  },
  "exports": {
    ".": "./src/server.ts",
    "./server": "./src/server.ts",
    "./tui": "./src/tui.tsx"
  },
  "engines": {
    "opencode": "^1.0.0",
    "bun": ">=1.1.0"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": "*",
    "@opentui/core": "*",
    "@opentui/solid": "*",
    "solid-js": "*"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.17.9",
    "@opencode-ai/sdk": "^1.18.18",
    "@opentui/core": "^0.4.1",
    "@opentui/solid": "^0.4.1",
    "@types/bun": "^1.3.14",
    "solid-js": "^1.9.13",
    "typescript": "^6.0.3"
  }
}
